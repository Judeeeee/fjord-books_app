<h1>New Icon</h1>

<%= render 'form', icon: @icon %>
<%= form_with model: @icon, local: true  do |form| %>
  <%= form.label :avatar %>
  <%= form.file_field :avatar %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Back', icons_path %>
